@echo off
echo This script is superseded - start_project.bat now starts the live
echo monitor (continuous capture + detection + alerting) automatically as
echo part of starting the whole project. Just run start_project.bat and
echo then attack from Kali whenever you're ready - nothing else to run here.
pause
